{
  "name": "tubescore",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:dev": "node scripts/build.mjs --dev-bootstrap",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify:build": "npm run build && node scripts/verify-build.mjs standard",
    "verify:build:dev": "npm run build:dev && node scripts/verify-build.mjs dev",
    "verify": "npm run typecheck && npm test && npm run verify:build && npm run verify:build:dev"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
